Bạn chỉ cần 2 nhánh cố định là staging và prod.

Khi PR merge và có label backport-to:staging, bot tự tạo PR sang staging.

Khi PR merge và có label backport-to:prod, bot tự tạo PR sang prod (hotfix trực tiếp).
sorenlouv/backport-github-action@v9.5.1sorenlouv/backport-github-action@v9.5.1sorenlouv/backport-github-action@v9.5.1sorenlouv/backport-github-action@v9.5.1


Bạn chỉ cần 2 nhánh cố định là staging và prod.

Khi PR merge và có label backport-to:staging, bot tự tạo PR sang staging.

Khi PR merge và có label backport-to:prod, bot tự tạo PR sang prod (hotfix trực tiếp).
sorenlouv/backport-github-action@v9.5.1sorenlouv/backport-github-action@v9.5.1sorenlouv/backport-github-action@v9.5.1sorenlouv/backport-github-action@v9.5.1\

Bạn chỉ cần 2 nhánh cố định là staging và prod.

Khi PR merge và có label backport-to:staging, bot tự tạo PR sang staging.

Khi PR merge và có label backport-to:prod, bot tự tạo PR sang prod (hotfix trực tiếp).
sorenlouv/backport-github-action@v9.5.1sorenlouv/backport-github-action@v9.5.1sorenlouv/backport-github-action@v9.5.1sorenlouv/backport-github-action@v9.5.1


Bạn chỉ cần 2 nhánh cố định là staging và prod.

Khi PR merge và có label backport-to:staging, bot tự tạo PR sang staging.

Khi PR merge và có label backport-to:prod, bot tự tạo PR sang prod (hotfix trực tiếp).
Khi PR merge và có label backport-to:prod, bot tự tạo PR sang prod (hotfix trực tiếp).
